# Open terminal
super + Return
    alacritty

# Launch Rofi
super + d
    rofi -show drun

# Open Browser
super + b
    google-chrome-stable

# Kill window (quit client)
super + shift + q
    pkill -KILL -f 'dwm|xinit'

